@model FlowerShop.Models.Topic

@{
    ViewBag.Title = "DetailCD";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}



<div>
    <div class="card shadow-sm mt-2 mx-3">
        <div class="card-body">
            <h4><i class="fa-solid fa-key text-success"></i> Tài khoản</h4>
        </div>
    </div>
    <div class="card mt-4 shadow-sm mx-3">
        <div class="card-body">
            <dl class="dl-horizontal">
                <dt>
                    @Html.DisplayNameFor(model => model.TopicName)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.TopicName)
                </dd>

            </dl>
        </div>
        <div>
            <a class="btn btn-warning text-dark" href="@Url.Action("EditCD", new { id = Model.TopicID })">Edit</a>
        </div>
        <p>
            @*@Html.ActionLink("Edit", "EditCD", new { id = Model.TopicID }) |*@
            @Html.ActionLink("Back to List", "ContentCD")
        </p>
    </div>
</div>